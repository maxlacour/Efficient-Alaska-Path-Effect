name: alaska_skip
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - numpy>=1.25
  - scipy>=1.11
  - pandas
  - scikit-learn>=1.3      # randomized_svd used in main loop
  - pyfftw                 # faster FFT on Apple Silicon (optional but recommended)
